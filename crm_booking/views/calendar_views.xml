<?xml version="1.0" ?>
<odoo>
    <!-- Hide Calendar menu when not in debug with assets mode -->
    <record model="ir.ui.menu" id="calendar.mail_menu_calendar">
        <field name="groups_id" eval="[(6,0,[ref('base.group_no_one')])]" />
    </record>
</odoo>
